# AGENTS.md

## 沟通与授权规则
- 默认使用中文，先讲结论再讲原因；术语、英文或缩写必须补一句白话解释。
- 步骤说明用编号，单次最多 3 步；需要用户决策时给 2-3 个选项并标注推荐项。
- 需求不明确时先问一个关键问题；完成后简短说明改了什么、改在哪、下一步可选动作。
- 删除、覆盖、联网安装等敏感操作先标注风险等级（低/中/高）。
- 发起授权请求（approval/escalation）前，必须先单独发送中文预告，优先控制在 4 行内。
- 授权预告至少说明：执行命令、白话用途、触发原因、影响/回退方式；若系统弹窗是英文，应说明英文来自工具界面。

## 日志与测试输出规则
- 项目日志文案默认使用中文；仅第三方接口/库返回的原始错误可保留英文原文。
- 日志必须按模块写入 `logs/`，文件名能看出来源，禁止所有模块共用单一日志文件。
- 日志级别清晰分层：`DEBUG` 排查细节、`INFO` 正常进度、`WARNING` 可恢复异常、`ERROR` 失败或需处理。
- 终端默认只展示关键日志（`WARNING/ERROR`、核心进度、最终结果）；详细日志写入模块日志文件。
- 执行失败时优先返回错误摘要、定位信息和下一步修复建议；需要完整日志时只给路径和关键片段。

## 代码开发与修改原则
- 默认采用简洁实现：优先简单、清晰、可维护的方案，避免过度设计。
- 避免硬编码：参数、方法、字段、变量命名、注释和业务逻辑应可配置、可复用、可读。
- 按职责模块化，减少耦合，避免循环依赖；高层模块通过接口/抽象协作，不依赖低层实现细节。
- 修改问题采用“最小必要改动”：只改与目标直接相关的代码，避免无关重构和不必要的元数据变动。

## 条件与循环使用规则
- `if` 是条件判断，`for` 是循环遍历；二者是基础语法，不禁止使用，但禁止用大量分散的 `if`/`for` 掩盖职责不清、规则散落或重复扫描的问题。
- 减少 `if`/`for` 不是机械统计数量，而是减少复杂分支、嵌套分支和混合职责循环；多层 `if` 优先用提前返回、小函数、门控模块、集中错误处理、状态机或枚举分发表达。
- 当分支只是“类型/错误码/状态码 -> 处理结果”时，优先使用规则表、映射表（`Map`，按 key 查 value 的表）、数组表、枚举 `match` 或集中映射函数。
- 复杂 `for` 循环应拆成“收集输入、规则判断、执行动作、汇总结果”等单一职责步骤；循环体不应同时做查询、判定、下单、记账、日志和 UI 展示。
- 迭代器 `map()` 表示“把一批数据逐个转换成另一批数据”，只适合无副作用的数据转换；不应用来隐藏下单、写账本、写日志、修改全局状态这类有副作用动作。
- 频繁循环查找同一类数据时，应考虑建立索引、缓存或状态对象，避免重复扫描；主链路和热路径可以保留直接、简单、低开销的 `if`/`for`，不要为了减少语法数量引入更慢或更绕的抽象。

## 注释语言与风格规则
- 新增或修改脚本时，注释默认使用中文，不使用英文注释。
- 注释内容尽量通俗易懂，优先解释“这段代码在做什么、为什么这样做”。
- 避免空泛注释和逐行翻译式注释，保留对新手有帮助的关键说明。

## 文件规模约束
- 单个模块文件建议不超过 800 行，主入口文件建议不超过 1500 行。
- 若接近或超过限制，优先按职责拆分，禁止只为减少行数搬运代码。

## 文档、测试与模拟相关规则
- 默认不主动编写文档和测试脚本，除非用户明确提出。
- 若用户明确要求创建文档或测试脚本，不要放在根目录；应放入对应类型目录（如 `docs/`、`tests/`）。
- 测试用例禁止写在生产实现文件中；Rust 测试放在 `xxx_tests.rs`、`tests.rs` 或 `tests/`，主实现文件只保留必要引用。
- 新增或迁移测试后，应确认非测试文件中没有 `#[test]`、`#[tokio::test]` 等测试用例标记；测试辅助函数可按最小范围保留 `#[cfg(test)]`。
- 除非用户主动要求，不编写测试网、模拟盘或模拟交易相关代码。

## 职责与架构边界规则（强制）
- 先按职责设计，再按文件拆分；拆文件不是目标，目标是让每个模块明确“负责什么、不负责什么、拥有哪些状态、输出什么结果”。
- 主流程只做编排：读取输入、调用模块、消费统一结果；禁止在主流程里直接堆业务规则、错误码分支、日志格式化、UI 展示或底层状态细节。
- 每个核心状态必须有明确所有者，例如 pending 成交、恢复计划、程序账本、风控状态、挂单状态、最近事件；外部模块通过方法或结果对象访问。
- 跨模块协作优先通过 `Decision`、`Report`、`Event`、`Command` 这类明确数据结构传递，禁止多个模块随意修改同一份可变状态。
- 策略判断、下单执行、成交确认、程序记账、持仓纠偏、强制平仓、风控判断、日志/UI 展示应保持职责分离；如果函数名或实现同时包含“查询、判断、执行、记账、打印、展示”等多个动作，应优先拆分。
- UI、监控、统计、日志属于旁路展示能力，默认只读取快照或事件；风控、错误处理、强制平仓、持仓纠偏作为独立能力接入主链路，主链路只消费它们输出的门控结果或执行计划。
- 架构重构必须服务于职责清晰、状态归属清晰、低耦合和低延迟；禁止只为减少行数或文件大小搬运代码，也不能为了抽象引入阻塞 I/O、额外网络请求、重日志、重复扫描或明显增加分配。

## 通用错误处理模块规则（强制）
- 下单失败重试必须走独立错误处理模块，禁止把错误码分支散落在策略/执行主流程。
- 错误处理模块只负责识别错误、给出一次性重试决策、返回可读原因；不承载策略逻辑，默认只重试一次。
- 针对性修复按错误码集中映射扩展（遇到一个补一个），不改主链路时序；余额/盘口补查只在匹配到对应错误后触发。
- 灰尘数量、最小下单量、滑点放大倍数等规则应参数化；新增错误处理规则必须同步中文日志，说明错误类型、决策动作、是否重试、最终结果。
- 主执行网关只做编排：调用错误处理模块并执行返回的决策，不直接内嵌复杂错误分支。

## 通用风控模块规则（强制）
- 风控逻辑必须独立成模块，禁止把风控分支散落在策略、执行、UI 等多个模块中。
- 风控模块只负责读取快照、判定规则、输出统一决策、返回可读原因；不直接下单。
- 风控规则必须配置化，结果必须标准化输出：`是否允许`、`阻断原因`、`建议动作`。
- 新增风控按规则项增量扩展，保持兼容；风控日志必须中文可读，包含规则名、触发条件、当前值、阈值、影响范围。
- 风控异常必须降级可控：默认保守阻断买入、允许必要平仓，且不能拖垮主链路。

## 主链路开发优先级规则（强制）
- 主套利链路（行情→判定→下单→成交确认）以“执行效率、低开销、低延迟、速度”作为最高优先级。
- 主链路只保留交易必需逻辑，避免冗余分支、重复计算和过度封装。
- 任何可能增加主链路延迟的功能必须先旁路化或缓存化；热路径禁止非必要 I/O、重日志、长字符串格式化、阻塞等待和高频外部查询。
- 主链路改动必须先性能后功能细节；如性能与展示/统计冲突，优先保障主链路速度，展示功能降级或延后执行。

## 套利程序专项约束（强制）
- 只要改动涉及套利程序、交易主链路、交易所适配器、行情、风控、执行、成交确认、持仓对账、UI 展示或监控统计，都必须遵守本文件中的主链路、风控、错误处理、日志、测试和低延迟规则；规则按功能性质生效，不依赖额外专项文档。
- 任一交易所可用余额低于阈值时，必须暂停触发套利并输出告警日志。
- 新功能默认可开关、可降级，异常不能拖垮主链路。
